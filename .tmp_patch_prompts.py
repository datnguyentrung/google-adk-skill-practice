from pathlib import Path
skill=Path(r'D:\Thuc_tap_MB\google-adk-skill-practice\app\skills\ingestion\SKILL.md')
s=skill.read_text(encoding='utf-8')
s=s.replace('1. Call `begin_ingestion(artifact_name)`. It returns at most 25 chunks and\n   16,000 source characters in `nextBatch` plus an `ingestionId` and a compact ontology catalog.','1. Call `begin_ingestion(artifact_name)`. It returns at most 12 chunks and\n   7,000 source characters in `nextBatch` plus an `ingestionId` and a compact ontology catalog.')
s=s.replace('''3. For every chunk in that batch, decide `MAPPED` or `NOT_RELEVANT`.\n   `NOT_RELEVANT` is exceptional: use it only when the chunk contains no\n   business fact that can be represented by the ontology.''','''3. For every chunk in that batch, decide `MAPPED` or `NOT_RELEVANT`.\n   `MAPPED` means the chunk contributes at least one distinct persisted property\n   or relationship fact. Use `NOT_RELEVANT` for duplicated examples, narrative,\n   repeated explanations, or thematically related text that adds no distinct\n   graph fact. Coverage means every chunk was reviewed, not that every chunk\n   must create graph data.''')
s=s.replace('''   if it is rejected. A response with `stage: "batch_validation"` and\n   `retryRequired: true` is not terminal: use the returned `nextBatch`, fix the\n   fragment, and call `submit_ingestion_batch` again for that same `batchIndex`\n   in this invocation. Read `errorSummary`, `repairInstructions`, and\n   `affectedChunks` before rebuilding the fragment.''','''   if it is rejected. A response with `stage: "batch_validation"` and\n   `retryRequired: true` is not terminal: repair the same batch using the source\n   chunks already present in the conversation, then resubmit that `batchIndex`.\n   Read `errorSummary`, `repairInstructions`, and `affectedChunkIndexes`; retry\n   responses intentionally do not repeat the whole batch payload.''')
s=s.replace('''Do not mark a chunk `NOT_RELEVANT` merely because another chunk already created\na node of the same class. A later chunk may contribute new properties, rules,\ndocuments, segments, scripts, campaigns, offers, or knowledge.''','''Do not mark a chunk `NOT_RELEVANT` merely because another chunk already created\na node of the same class if the later chunk adds a distinct persisted fact. But\nif the later chunk is only a duplicate example or repeated explanation and adds\nno distinct graph fact, `NOT_RELEVANT` is correct.''')
skill.write_text(s,encoding='utf-8')
root=Path(r'D:\Thuc_tap_MB\google-adk-skill-practice\app\prompts\root_agent_prompt.md')
r=root.read_text(encoding='utf-8')
r=r.replace('`errorSummary`, `repairInstructions`, `affectedChunks`, and `nextBatch`.','`errorSummary`, `repairInstructions`, and `affectedChunkIndexes`.')
r=r.replace('''- Do not ask the user to wait while a loaded skill still has a retry, finalize,\n  validation, or fill step available in the same invocation.''','''- Do not ask the user to wait while a loaded skill still has a retry, finalize,\n  validation, or fill step available in the same invocation. Never claim you\n  will continue processing after sending the response; there is no background\n  execution. Reach a real terminal state or report an explicit terminal failure.''')
root.write_text(r,encoding='utf-8')
print('prompts updated')